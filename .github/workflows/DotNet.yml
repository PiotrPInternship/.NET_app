name: dotnet build

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
        - name: Setup .NET 3.x
          uses: actions/setup-dotnet@v2
          with:
            dotnet-version: '3.x'
